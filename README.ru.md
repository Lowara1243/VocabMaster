# Gemini Word Processor

[English](README.md) | Русский

Клиент-серверное приложение для получения детальной информации об английских словах и фразах с помощью Google Gemini.

- **Бэкенд**: API на FastAPI, обрабатывающее слова через `gemini-cli`
- **Веб-клиент**: React + Vite приложение с минимальными зависимостями

## ✨ Возможности

- Обработка отдельных слов и фраз (например, "look for")
- Поддержка контекста для более точного перевода (например,`[a piece of furniture] table`)
- Поддержка 10 популярных языков со специфическими правилами:
  - **Немецкие существительные**: Автоматическое добавление артикля и формы множественного числа (например, "Apfel" → "der Apfel (die Äpfel)")
  - **Английские глаголы**: Автоматическое добавление частицы "to" (например, "go" → "to go")
- Стриминг результатов в реальном времени (асинхронная обработка) с контролем конкурентности
- Сохранение состояния: выбранные языки и история слов сохраняются после обновления страницы
- Получение транскрипции (IPA), вариантов перевода и примеров использования
- Сохранение результатов в CSV-файл, оптимизированный для **ReWord** (а также Anki и других)
- Минимальный размер зависимостей

## ⚙️ Требования

- **Python 3.10+**
- **uv** (для управления зависимостями Python)
- **Node.js 18+** (для веб-клиента)
- **Gemini CLI**: утилита `gemini` должна быть установлена и доступна в PATH (проверьте `gemini -h`)
- **screen**: необходим для работы серверов в фоновом режиме (только для Linux)
  - *Debian/Ubuntu*: `sudo apt install screen`
  - *Fedora*: `sudo dnf install screen`
  - *Arch*: `sudo pacman -S screen`

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/Lowara1243/VocabMaster
cd VocabMaster/
```

### 2. Настройте Python окружение

```bash
uv venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate на Windows
```

### 3. Установите зависимости

```bash
# Python зависимости (backend)
uv sync

# Frontend зависимости (если используете веб-клиент)
cd frontend
npm install
cd ..
```

### 4. Настройте переменные окружения

#### Для веб-клиента:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите URL API:

```bash
VITE_APP_API_URL=http://127.0.0.1:8000/process-words
```

### Вариант 1: Быстрый запуск (Рекомендуется)

Запустите следующий скрипт для автоматической настройки зависимостей (при первом запуске) и одновременного запуска бэкенда и фронтенда:

```bash
chmod +x run_project.sh
./run_project.sh
```

### Вариант 2: Ручной запуск

```bash
# Рекомендуемый способ
uv run uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000

# Или через helper скрипт
uv run python backend/run.py
```

Сервер запустится на `http://127.0.0.1:8000`

#### Шаг 2: Запустите фронтенд

В новом терминале:

```bash
cd frontend
npm install
npm run dev
```

Откройте браузер и перейдите на `http://localhost:5173`

### Примеры использования

1. Введите слова через запятую в текстовое поле.
2. Для более точного перевода можно указать контекст в квадратных скобках, например: `[a piece of furniture] table`
3. Выберите исходный и целевой языки
4. Нажмите "Process Words"
5. Результаты отобразятся в реальном времени
6. История сохраняется автоматически в браузере
7. Скачайте CSV с помощью кнопки "Download CSV"

## 📁 Структура проекта

```
.
├── backend/
│   ├── main.py              # FastAPI сервер
│   └── prompts/             # Шаблоны промптов для Gemini
│       ├── prompt.txt
│       └── fix_json_prompt.txt
├── frontend/
│   ├── src/
│   │   ├── App.jsx          # Главный компонент
│   │   ├── utils.js         # CSV и форматирование
│   │   ├── simple.css       # Минимальный CSS
│   │   └── components/
...
│   └── package.json         # Только React + Vite
├── pyproject.toml           # Python зависимости
└── README.md                # Этот файл
```

## 🔧 Конфигурация

**Backend:**
- `GEMINI_MODEL` - модель Gemini (default: gemini-2.5-flash)
- `MAX_CONCURRENT_REQUESTS` - максимальное количество одновременных запросов к Gemini (default: 5).

**Frontend (для production):**
- `VITE_APP_API_URL` - URL бэкенда (default: http://127.0.0.1:8000/process-words)

## 🎯 Формат CSV

Результаты сохраняются в CSV с разделителем `;`, оптимизированном для прямого импорта в **ReWord**:

| Слово | Транскрипция | Переводы | Примеры (Источник; Перевод; ...) |
|-------|--------------|----------|----------------------------------|

Примеры содержат выделение изучаемого слова символами # для удобного импорта в ReWord или Anki.

### 🗃️ Поддержка Anki (.apkg)

Если вы предпочитаете использовать **Anki**, вы можете конвертировать скачанный CSV в нативный пакет Anki (`.apkg`) с готовым оформлением:

```bash
uv run python scripts/csv_to_anki.py result.csv -o my_deck.apkg -n "Мой английский словарик"
```

Это создаст колоду с профессиональной версткой, транскрипцией и примерами.

## 🧪 Разработка

```bash
# Форматирование и линтинг
cd frontend
npm run lint

# Production build
npm run build

# Preview production
npm run preview
```

## 📝 Примечания

Для получения дополнительной информации о проекте, пожалуйста, обратитесь к [документу со спецификациями](docs/RU/specifications.md).

## 🔗 Полезные ссылки

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [Gemini CLI](https://github.com/google/generative-ai-cli)

## 📄 Лицензия

Этот проект создан для образовательных целей.
