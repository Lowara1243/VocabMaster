# Спецификации

## 📝 Примечания

- Бэкенд **должен** использовать `gemini-cli` через подпроцесс (ограничение интеграции с аккаунтом Google)
- CSV-файлы используют UTF-8 с BOM для совместимости с Excel
- Веб-клиент сохраняет историю в localStorage браузера
- Консольный клиент добавляет результаты в один CSV-файл
- Оба клиента могут работать одновременно с одним бэкендом
- При работе с разными языками необходимо учитывать их специфические языковые особенности (например, капитализацию существительных в немецком языке, наличие артиклей и т.д.) и при необходимости адаптировать промпты для `gemini-cli`.
